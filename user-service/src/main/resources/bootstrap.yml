spring:
  application:
    name: user-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      name: common
      profile: ${spring.profiles.active}
  profiles:
    active: dev

