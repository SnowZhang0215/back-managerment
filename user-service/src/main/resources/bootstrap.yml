spring:
  application:
    name: core-gateway
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      name: common
      profile: ${spring.profiles.active}
  profiles:
    active: dev

